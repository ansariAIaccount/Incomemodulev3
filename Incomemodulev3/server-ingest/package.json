{
  "name": "pcs-ingestion-service",
  "version": "1.0.0",
  "description": "Standalone document ingestion + AI extraction service for the PCS Loan Module V3. Runs alongside (not inside) the Investran DIU proxy so that long LLM calls, PDF handling, and future email/webhook features don't compete with the DIU push path.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
